package service

/*
import (
	"github.com/gin-gonic/gin"
	"net/http"
)

func Reload(ctx *gin.Context) {
	err := Services.ConfigService.Reload()
	if err != nil {
		Services.ServiceLogger.Error(err.Error())
		ctx.String(http.StatusInternalServerError, err.Error())
		return
	}
	Services.ServiceLogger.Info("Config reload success")
	ctx.String(http.StatusOK, "Reload finish")
	return
}
*/
